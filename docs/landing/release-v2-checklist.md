# Release checklist для лендинга v2

Документ фиксирует обязательные шаги перед прод-выкладкой и сразу после неё.

## 1) Контент-ревизия
- Проверить актуальность всех текстов на страницах `web/` (офферы, дисклеймеры, тарифы, FAQ, контакты).
- Убедиться, что Telegram CTA ведёт на корректный deep-link бота.
- Проверить, что служебные плейсхолдеры **не остались в прод-контенте**.

## 2) SEO-ревизия
- Проверить `<title>`, `meta description`, canonical, OpenGraph/Twitter-теги на ключевых страницах.
- Проверить индексацию: публичные страницы открыты, страницы `web/legal/*` закрыты от индексации по зафиксированной политике (`noindex,follow` + `Disallow: /legal/` + отсутствие в sitemap).
- Сверить внутренние ссылки между страницами (`/`, `/prices/`, `/faq/`, `/contacts/`, `/articles/`).

## 3) Ключи (100/100)
- Пройти финальный прогон по SEO/контент ключам и зафиксировать статус **100/100**.
- Сверить план новых/обновляемых статей с `docs/landing/seo-keyword-map.md`: по каждому ключу должен быть ровно один целевой URL под конкретный интент.
- Для новых URL закрыть контент-чек: **«новый URL не дублирует существующий кластер»**.
- Убедиться, что в текстах нет временных/черновых формулировок и «TODO».

## 4) robots/sitemap
- Проверить `meta robots` на legal-страницах: в `web/legal/*/index.html` должно быть `noindex,follow`.
- Проверить `web/robots.txt`: корректный `Host`, `Sitemap`, наличие `Disallow: /legal/` и отсутствие конфликтов с целевой стратегией.
- Проверить `web/sitemap.xml`: домен прода, только актуальные URL, без дублей и без `https://aireadu.ru/legal/*`.

## 5) JSON-LD
- Проверить наличие и корректность JSON-LD на страницах, где он предусмотрен (например, FAQ/Offer/Service).
- Проверить валидность разметки (структура полей, корректный домен, отсутствие плейсхолдеров).

## 6) A11y
- Проверить базовую доступность: контраст, читаемость, фокус при навигации с клавиатуры, alt/aria на ключевых элементах.
- Убедиться, что интерактивные элементы доступны без мыши и видимы в фокусе.

## 7) Smoke-check CTA после выкладки
1. Выполнить smoke-check:
   ```bash
   LANDING_URL="https://aireadu.ru/" \
   LANDING_EXPECTED_CTA="https://t.me/AIreadUbot" \
   LANDING_ASSET_URLS="https://aireadu.ru/assets/css/styles.css,https://aireadu.ru/assets/js/script.js" \
   ./scripts/smoke_check_landing.sh
   ```
2. Ручной обход ключевых страниц после деплоя:
   - `https://aireadu.ru/`
   - `https://aireadu.ru/prices/`
   - `https://aireadu.ru/faq/`
   - `https://aireadu.ru/contacts/`
   - `https://aireadu.ru/articles/`

## Обязательный шаг релиза перед продом
Перед прод-выкладкой обязательно:
- убедиться, что на лендинге используется домен `https://aireadu.ru`;
- проверить, что все CTA ведут на `https://t.me/AIreadUbot`;
- заменить технические заглушки `будет добавлено` на финальные значения.

Релиз блокируется до полной замены технических заглушек.
